[Coding Aider Plan - Checklist]

- [x] Add `import numpy as np` to `analysis_lib/config_defaults.py`.
- [ ] Run existing unit tests (`tests/test_analysis_modules.py`) to confirm no regressions.
- [ ] Manually verify that the application runs without the `Unresolved reference 'np'` error.
- [ ] Review `config_defaults.py` to ensure `numpy` is used appropriately for conversions.
